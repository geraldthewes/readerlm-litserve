# Core ML dependencies
torch>=2.9.0
transformers>=5.0.0

# Quantization support (VRAM optimization)
bitsandbytes>=0.49.1
accelerate>=1.12.0

# HTML preprocessing (content extraction)
readability-lxml>=0.8.4
lxml>=5.0.0

# Server framework
litserve>=0.2.16

# HTTP client (for client.py)
requests>=2.32.5

# Async HTTP client (for URL fetching)
httpx>=0.28.0

# Terminal formatting
rich>=14.3.0

# Environment configuration
python-dotenv>=1.2.0
